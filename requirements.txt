# Uses the toolkit's venv (shared deps, avoids reinstalling databricks-connect etc.)
# One-time setup:
#   .venv/bin/python -m pip install -e ../mlops_toolkit
#   .venv/bin/python -m pip install streamlit plotly PyGithub
# Then run:
#   cp .env.example .env  # fill in your workspace creds
#   .venv/bin/python -m db.setup
#   .venv/bin/streamlit run app.py

streamlit>=1.35.0
plotly>=5.22.0
databricks-sdk>=0.20.0
mlflow>=2.10.0
pandas>=2.1.0
pyarrow>=15.0.0
pydantic>=2.0.0
pyyaml>=6.0.0
python-dotenv>=1.0.0
PyGithub>=2.3.0
# Step 3 "Profile Data" (owner request 2026-07-13). Renamed twice —
# pandas-profiling -> ydata-profiling (2023) -> fg-data-profiling (2026-04,
# confirmed against current PyPI). Installed and pip-audited clean
# (2026-07-13).
fg-data-profiling>=4.18.4
# fg-data-profiling imports pkg_resources directly but doesn't declare
# setuptools as a dependency. setuptools>=81 dropped the pkg_resources
# shim, which breaks full_profile() at runtime (ModuleNotFoundError).
# Pin until fg-data-profiling migrates off pkg_resources.
setuptools<81
